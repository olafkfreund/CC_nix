# MkDocs config for Backstage TechDocs.
# Edit at will — Backstage rebuilds on next view.
# https://backstage.io/docs/features/techdocs/getting-started

site_name: CC_nix
site_description: CC config for working with nixos and nixpkgs. A place to share what works and what does not.

docs_dir: docs

plugins:
  - techdocs-core
